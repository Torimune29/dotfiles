# C-xC^x open this on bash.
# sharp, [.*] strip automatically.

# utility
[utility  : search word on google] google hello
[utility  : check alias          ] alias | fzf
[utility  : open+push snippets   ] code --wait ~/.snippets && chezmoi add ~/.snippets
[utility  : clip history         ] history | sort -rn | sed -e 's/ *[0-9][0-9]* *//' | fzf | pbcopy
[utility  : copy linux to win    ] cat file | pbcopy
[utility  : paste win to linux   ] pbpaste | cat > file
[utility  : get specific raw     ] cat file | grep URL | cut -d " " -f 2
[utility  : change tab to space 2] expand -t 2 file.txt

# markdown
[markdown : conv space-sv 2 table] pbpaste | sed -e 's/\ /|/g' -e 2i$(pbpaste | head -n 1 | sed -e 's/ /|/g' -e 's/\w*/--/g')

# code
[code     : open remote directory] code --file-uri vscode-remote://ssh-remote+host/home/user


# windows
[windows  : open workspace dir   ] code $WINDOWS_USER_HOME_WSL_PATH/00.workspace
[windows  : open file as windows ] open ~/.snippets

# stats
[stats    : vmstat with timestamp] vmstat 1 | awk '{print strftime("%y/%m/%d %H:%M:%S"), $0}'
[stats    : listen ports         ] lsof -Pan -i tcp -i udp

# dotfiles
[dotfiles : open dotfiles workdir] code $(chezmoi source-path)

# my_own
[my_own   : check log dir        ] ls -al $MY_OWN_LOG_PATH/terminal

