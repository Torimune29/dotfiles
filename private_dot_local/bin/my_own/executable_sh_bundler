#!/bin/sh


if [ $# -lt 1 ]; then
  echo "error: argument is empty." >&2
  return 1
fi
for i; do
  if [ ! -e "$i" ]; then
    echo "error: $i not found." >&2
    return 2
  fi
done
for i; do
  < "$i" sed 's/^#!\/[^ ]*\/\(ba\|\)sh\( *\)//'
  echo ""
done


