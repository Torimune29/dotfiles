#!/bin/bash
# see: https://qiita.com/rsooo/items/ef1d036bcc7282a66d7d

DIR="$(cd $(dirname $0)/; pwd)"
. "$DIR"/_log

if [ -n "${MY_OWN__DEBUG}" ]; then
  trap 'read -p "next(LINE:$LINENO)>> log::warning $BASH_COMMAND"' DEBUG;
fi