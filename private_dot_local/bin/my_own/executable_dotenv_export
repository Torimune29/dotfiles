#!/usr/bin/env sh

set -e

if [ $# -lt 1 ]; then
  return 1
fi
for i; do
  if [ ! -e "$i" ]; then
    return 2
  fi
done
for i; do
  sed '/^[[:blank:]]*#/d;s/[[:blank:]]*#.*//' $i | while read line; do
    eval export "$line"
  done
done


